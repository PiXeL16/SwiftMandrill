platform :ios, ‘9.0’
use_frameworks!

plugin 'cocoapods-keys'

pod 'Alamofire', '~> 4.3.0’
pod 'ObjectMapper', '~> 2.2.2’


target 'SwiftMandrillTests' do
  pod 'Quick', '~> 1.0.0'
  pod 'Nimble', '~> 5.1.1'

plugin 'cocoapods-keys', {
  :project => "SwiftMandrill",
  :keys => [
    "MANDRILL_API"  
]}

end

